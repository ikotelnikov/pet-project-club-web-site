name: CI

on:
  push:
    branches: ["main"]
    paths-ignore:
      - "content/**"
      - "assets/**"
      - "robots.txt"
      - "sitemap.xml"
  pull_request:
    paths-ignore:
      - "content/**"
      - "assets/**"
      - "robots.txt"
      - "sitemap.xml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Prepare test result directory
        run: mkdir -p ci-results test-results

      - name: Start CI test timer
        run: echo "CI_TEST_STARTED_SECONDS=$(date +%s)" >> "$GITHUB_ENV"

      - name: Run bot tests
        id: bot_tests
        continue-on-error: true
        run: |
          start=$(date +%s)
          set +e
          npm run bot:test:junit
          status=$?
          end=$(date +%s)
          echo "NODE_TEST_WALL_SECONDS=$((end - start))" >> "$GITHUB_ENV"
          exit "$status"

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium webkit

      - name: Generate localized pages
        run: npm run build:locales

      - name: Run Playwright tests
        id: playwright_tests
        continue-on-error: true
        run: |
          start=$(date +%s)
          set +e
          npx playwright test --reporter=json > ci-results/playwright-tests.json
          status=$?
          end=$(date +%s)
          echo "PLAYWRIGHT_TEST_WALL_SECONDS=$((end - start))" >> "$GITHUB_ENV"
          exit "$status"

      - name: Report results to TestRail
        if: always()
        env:
          TESTRAIL_HOST: ${{ secrets.TESTRAIL_HOST }}
          TESTRAIL_USERNAME: ${{ secrets.TESTRAIL_USERNAME }}
          TESTRAIL_API_KEY: ${{ secrets.TESTRAIL_API_KEY }}
          TESTRAIL_PROJECT_ID: "3"
          TESTRAIL_SUITE_ID: "11"
          NODE_TEST_RESULTS: ci-results/node-tests.xml
          PLAYWRIGHT_TEST_RESULTS: ci-results/playwright-tests.json
        run: node tools/report-testrail-results.mjs

      - name: Fail if tests failed
        if: always() && (steps.bot_tests.outcome == 'failure' || steps.playwright_tests.outcome == 'failure')
        run: exit 1

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
